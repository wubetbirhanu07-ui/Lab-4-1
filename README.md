. Efficiency & Logic: push() and filter() simplified data management, though ensuring case-insensitive matches during the search process required careful string normalization.
2. Improvements: To enhance UX, I would add a "Delete" button to individual items and an event listener to allow adding items via the "Enter" ke
